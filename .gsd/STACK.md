# Technology Stack

> Generated by /map on 2026-03-24

## Runtime
| Technology | Version | Purpose |
|------------|---------|---------|
| Node.js | v20 LTS | Authentic REST API boundary mapping PostgreSQL logic |
| Rust | 1.93.1 | Extreme performance API proxy logic / semantic caching gateway |
| Python | 3.11 | LLM data science processing environment executing the heavy lifting |
| Vite + React | ES6+ | SPA client UI |

## Production Dependencies
| Package | Ecosystem | Purpose |
|---------|---------|---------|
| express, pg, bcrypt | npm | Authentication, Postgres sessions mapping, API HTTP bounds |
| axios | npm | Node-to-Rust and Vue-to-Node network interfacing |
| tokio, axum | cargo | Asynchronous server mapping incoming queries to Rust struct contexts |
| failsafe, tower_governor | cargo | Guard-railed rate limits and downstream circuit breaking |
| arq, asyncpg, redis | pip | High speed Redis concurrency loops matching Python function outputs to async PostgreSQL routines |
| pydantic, pyyaml, requests | pip | Configuration validations parsing hierarchical master_settings.yml structs |
| openai, ollama | pip | Native SDK API integrations parsing semantic prompts |

## Infrastructure
| Service | Provider | Purpose |
|---------|----------|---------|
| PostgreSQL | DB Engine | State persistence mapped explicitly to conversation boundaries |
| Redis | Memory Cache | `sb_redis` handles Semantic hits/misses locking timeouts to 7-days and executing ARQ background hashes |
| SearXNG | Web Fetcher | Pulling raw web domains dynamically into local scope |
| Ollama | LLM Engine | `sb_ollama` handles offline text classification locally scaling safely |

## Configuration
| Variable | Purpose | Required |
|----------|---------|----------|
| MASTER_CONFIG_PATH | Defines absolute structural YAML loading | No (defaults safely) |
| WARDEN_CONFIG_PATH | Overrides node parameters targeting isolated Rust setups | No |
| RUST_LOG | Tracing granularity | Yes |
| REDIS_PASSWORD | Enforces TLS-like caching encryptions internally | Yes |
| BUILD_PROFILE | Toggles `docker compose build warden` between `release` vs `debug` targets natively | No |
