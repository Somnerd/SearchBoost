---
phase: 2
plan: 9
wave: 1
depends_on: [2.8]
files_modified:
  - searchboost_ui/src/pages/Search.jsx
  - searchboost_warden/src/relay.rs
autonomous: true
must_haves:
  truths:
    - "UI continues polling on 429 response"
    - "Warden rate limit is relaxed"
---

# Plan 2.9: Web Stack Stabilization (Poll Resilience + Warden Tuning)

<objective>
Stabilize the UI polling and Warden rate limiting to handle long-running LLM tasks (30s+) without user-facing errors.

Purpose: 
- Prevent 429 errors from aborting the search state in React.
- Relax Warden's burst limit to accommodate polling overhead.
Output:
- Resilient Search UI.
- Tuned Warden relay.
</objective>

<context>
- searchboost_ui/src/pages/Search.jsx
- searchboost_warden/src/relay.rs
</context>

<tasks>

<task type="auto">
  <name>Update Search UI Polling Resilience</name>
  <files>searchboost_ui/src/pages/Search.jsx</files>
  <action>
    Modify the `catch` block in the search polling interval to check for 429 status.
    If 429 is detected, do NOT clear interval or set error. Simply log and wait for next tick.
    Only clear interval on other terminal errors (5xx, etc).
  </action>
  <verify>Check source for 429 handling logic.</verify>
  <done>UI ignores 429 during poll.</done>
</task>

<task type="auto">
  <name>Relax Warden Rate Limiter</name>
  <files>searchboost_warden/src/relay.rs</files>
  <action>
    Increase `per_second` to 25 and `burst_size` to 100 in `tower-governor` config.
  </action>
  <verify>Check source code for updated values.</verify>
  <done>Warden limits are relaxed.</done>
</task>

<task type="auto">
  <name>Rebuild and Final Verification</name>
  <files>docker-compose.yml</files>
  <action>
    Rebuild Warden and UI containers.
    Perform end-to-end verification.
  </action>
  <verify>docker ps shows all containers UP.</verify>
  <done>System stable and operational.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] No 429 errors abort the UI search state.
- [ ] Worker tasks finish and results display in UI.
</verification>

<success_criteria>
- [ ] UI polling survives 429.
- [ ] Warden rate limits tuned.
- [ ] Worker reliability verified.
</success_criteria>
