---
phase: 5
plan: 2
wave: 2
depends_on: ["5.1"]
must_haves:
  truths:
    - "Users visibly traverse historical chat threads via dynamic Sidebar interface"
  artifacts:
    - path: "searchboost_ui/src/pages/Search.jsx"
      provides: "Thread state hook mapping Sidebar & main query flow"
  key_links:
    - from: "Search.jsx"
      to: "/search/history/:currentThreadId"
      via: "Axios client.get wrapper fetching individual threads"
---
# Plan 5.2: React Concurrent Session Sidebar

## Objective
Introduce a visually stunning historical sidebar that manages multiple query contexts allowing users to traverse through and revive previous search conversations instantly.

## Tasks

<task type="auto">
  <name>React Thread Navigation State</name>
  <files>searchboost_ui/src/pages/Search.jsx</files>
  <action>
    - Establish robust local state to track absolute Active Thread (`[currentThreadId, setCurrentThreadId]`) and History list containing distinct `session_id`s compiled via the `/sessions` route.
    - Implement a `useEffect` dynamically calling `client.get('/search/history/:currentThreadId')` every time the active thread changes, preventing visual session clumping.
    - Inject `currentThreadId` seamlessly into the enqueue `client.post` wrapper.
  </action>
  <verify>Ensure `const [currentThreadId` is strictly injected into the node request.</verify>
  <done>Frontend accurately holds Thread state natively over re-renders.</done>
</task>

<task type="auto">
  <name>UI Sidebar Construction</name>
  <files>searchboost_ui/src/pages/Search.jsx</files>
  <action>
    - Convert `className="page-enter"` container into a structural layout hosting a left-aside `<Sidebar>` panel showing user's past query threads mapped beautifully using `var(--accent)` selection toggles.
    - Move "Start New Conversation" button into the prominent upper Sidebar, initiating `setCurrentThreadId(uuid.v4())` and dropping local history visually until the next query persists it.
  </action>
  <verify>Examine CSS structure inline injecting responsive Grid/Flex mapping.</verify>
  <done>Clean dual column chat interaction mimics modern models.</done>
</task>

## Success Criteria
- [ ] Users natively experience multiple separated interactions that never visually or logically intersect.
- [ ] UI is robust, updating instantly upon Thread swaps.
