---
phase: 2
plan: 10
wave: 1
depends_on: [2.9]
files_modified:
  - searchboost_api/src/db/history.js
  - searchboost_api/src/routes/search.js
  - searchboost_ui/src/pages/Search.jsx
autonomous: true
must_haves:
  truths:
    - "User can view their previous search history on page load"
    - "History is isolated by username (session_id)"
---

# Plan 2.10: Chat History Integration

<objective>
Implement persistent search history in the UI by fetching previous conversation turns from the database on page load.

Purpose: 
- Persistence of user queries and AI answers across sessions.
- Improved UX by showing context.
Output:
- API endpoint for history retrieval.
- UI logic to fetch and render history.
</objective>

<context>
- searchboost_service/searchboost_src/models.py (Database schema ref)
- searchboost_api/src/routes/search.js
- searchboost_ui/src/pages/Search.jsx
</context>

<tasks>

<task type="auto">
  <name>Create History DB Module</name>
  <files>searchboost_api/src/db/history.js</files>
  <action>
    Implement `getHistory(username)` which queries the `conversation_turns` table.
    Reconstruct pairs of {query, result} by matching sequential User and Assistant turns.
  </action>
  <verify>Check file for SQL query logic.</verify>
  <done>DB layer implemented.</done>
</task>

<task type="auto">
  <name>Expose History API Endpoint</name>
  <files>searchboost_api/src/routes/search.js</files>
  <action>
    Add `GET /history` endpoint (protected by `verifyToken`).
    Calls `getHistory(req.user.username)` and returns the results.
  </action>
  <verify>curl -G http://localhost:3001/api/search/history</verify>
  <done>API endpoint functional.</done>
</task>

<task type="auto">
  <name>Fetch History on UI Mount</name>
  <files>searchboost_ui/src/pages/Search.jsx</files>
  <action>
    Add a `useEffect` to fetch history from `/api/search/history` when the component mounts.
    Update `conversationHistory` state with the fetched data.
  </action>
  <verify>Visual check: History appears on refresh.</verify>
  <done>UI displays persistent history.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Logged in user sees their own history.
- [ ] History persists after page refresh.
</verification>

<success_criteria>
- [ ] Persistent chat history functional.
</success_criteria>
