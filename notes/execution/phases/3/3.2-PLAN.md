---
phase: 3
plan: 2
wave: 1
---

# Plan 3.2: Warden Observer & UI Chat History Fixing

## Objective

Fix the Rust-based Warden Docker socket observer so it retains proper log framing or timestamps when monitoring the worker container. Correct the UI or Python logic that pulls all chat history into a single blob vs breaking them apart by session or chat window.

## Context

- .gsd/SPEC.md
- searchboost_warden/src/observer.rs
- searchboost_service/searchboost_src/database.py OR searchboost_service/searchboost_src/service.py OR React Frontend source

## Tasks

<task type="auto">
  <name>Add Timestamps to Warden Observer</name>
  <files>searchboost_warden/src/observer.rs</files>
  <action>
    Edit `observer.rs` where it fetches log lines from `bollard` for the container stream.
    - Set the `<bollard::container::LogsOptions>` `timestamps` explicitly to `true` if not already set, taking the Docker daemon's attached iso8601 timestamps and writing them cleanly.
  </action>
  <verify>Check `searchboost_warden/src/observer.rs` for `timestamps: true` or manual timestamp insertion.</verify>
  <done>Observer outputs log files that have timestamps.</done>
</task>

<task type="auto">
  <name>Correct (UI) Chat History Context Leak</name>
  <files>searchboost_service/searchboost_src/service.py (or history logic)</files>
  <action>
    Investigate how `HistoryService` loads conversation turns.
    - Currently, it appears to merge *all* of a user's previous prompts.
    - The `session_id` should probably distinguish a specific thread, not just the user ID, or there should be a window limit (e.g., last 10 messages).
    - Investigate `SearchBoostService.run()` where `history_svc.load_history(self.session_id)` is invoked. Define a fix (either limit retrieval window or scope to a sub-session UUID).
  </action>
  <verify>Review how history is retrieved.</verify>
  <done>User questions in new conversations don't bleed into historical conversations from 30 minutes ago.</done>
</task>

## Success Criteria

- [ ] Warden observer logs correctly prefix each line with the exact timestamp from Docker.
- [ ] AI research sessions remain contextually isolated and don't leak global past queries to Ollama context windows on unrelated searches.
