# Plan 3.2: Warden Observer & UI Chat History Fixing (Summary)

**Tasks Completed:**
1. Rust `observer.rs`: Added `timestamps: true` into `LogsOptions` for `bollard` config, aligning container output with ISO8601 formatting.
2. Python `database.py`: Enforced a hard 15-minute `max_age_minutes` constraint on SQLAlchemy history retrieval via `created_at` timestamp deltas.

**Result**: PASS
