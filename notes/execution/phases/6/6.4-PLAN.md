---
phase: 6
plan: 4
wave: 3
depends_on: ["6.2"]
files_modified: ["searchboost_api/src/routes/search.js", "searchboost_api/src/db/history.js", "searchboost_service/searchboost_src/service.py"]
autonomous: true

must_haves:
  truths:
    - "No human can observe any history ID belonging to another API-token."
    - "SQL DB drops any wildcard search params injected aggressively."
    - "Assistant returns actively populate identical History blocks during redis checks."
---

# Plan 6.4: Core Logic Security & Cache Bypass

<objective>
Secure all functional vulnerabilities detected by static analysis that natively broke logic rules or allowed API side-channeling.

Purpose: Fixes IDOR and fixes missing chat logs directly validating our failed QA tests.
</objective>

<tasks>

<task type="auto">
  <name>Warden Result Proxy Hardening</name>
  <files>searchboost_api/src/routes/search.js</files>
  <action>
    Introduce severe logic inside `/result/:job_id` validating the requesting `req.user.username` against the internal contents of `job_id` prior to executing axos.get to Warden.
    Extract the prefix matching /^SB-SESSION-[^:]+/ and return 403 on mismatch.
  </action>
  <verify>grep "startsWith(\`SB-SESSION" searchboost_api/src/routes/search.js</verify>
  <done>IDOR fully blocked.</done>
</task>

<task type="auto">
  <name>LIKE Clause Injection Nullification</name>
  <files>searchboost_api/src/db/history.js</files>
  <action>
    Prior to generating `const prefix = ...`, execute javascript filtering `escapedUsername = username.replace(/[\\%_]/g, '\\$&')`.
    Pass the properly shielded parameter appending `ESCAPE '\\'` on the DB end.
  </action>
  <verify>grep "ESCAPE '\\\\'" searchboost_api/src/db/history.js</verify>
  <done>Wildcards are converted successfully to literals</done>
</task>

<task type="auto">
  <name>Semantic State Sync</name>
  <files>searchboost_service/searchboost_src/service.py</files>
  <action>
    During the `post_opt_cache` cache-hit intercept, artificially dump the found content into PostgreSQL exactly as if the LLM ran it manually.
    ```python
    if history_svc and self.session_id:
        await history_svc.save_turn(self.session_id, "assistant", post_opt_cache)
    ```
  </action>
  <verify>grep "save_turn" searchboost_service/searchboost_src/service.py</verify>
  <done>Assistant responses are completely persistent across Cache layers</done>
</task>

</tasks>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
