---
phase: 6
plan: 3
wave: 2
depends_on: []
files_modified: ["searchboost_api/Dockerfile", "searchboost_ui/Dockerfile", "searchboost_ui/nginx.conf"]
autonomous: true

must_haves:
  truths:
    - "System blocks processes originating from UID 0 / root permissions"
    - "React app is natively served out of ports 1024+ inside docker proxy"
---

# Plan 6.3: Docker Non-Root Identity Fixes

<objective>
Enforce strict boundaries globally so that if an LLM execution break or network flaw is discovered, attackers only capture lowest level kernel access and are restricted by OS bounds.

Purpose: Matches DevSecOps requirements.
</objective>

<tasks>

<task type="auto">
  <name>Sever API Root Authority</name>
  <files>searchboost_api/Dockerfile</files>
  <action>
    Add a manual `USER node` block at the bottom of the script, directly pre-pending the `CMD ["node", "src/app.js"]` logic.
    Ensure `chown -R node:node /app` is piped directly during copy.
  </action>
  <verify>grep "USER node" searchboost_api/Dockerfile</verify>
  <done>API is permanently neutered from system binaries.</done>
</task>

<task type="auto">
  <name>Sever UI Root Authority</name>
  <files>searchboost_ui/Dockerfile, searchboost_ui/nginx.conf</files>
  <action>
    Change `FROM nginx:stable-alpine` to `nginxinc/nginx-unprivileged:stable-alpine`.
    Expose 8080 instead of 80.
    Edit nginx.conf to `listen 8080`.
  </action>
  <verify>grep "8080" searchboost_ui/Dockerfile</verify>
  <done>UI correctly binds without root capabilities.</done>
</task>

</tasks>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
