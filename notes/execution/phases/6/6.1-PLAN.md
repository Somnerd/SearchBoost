---
phase: 6
plan: 1
wave: 1
depends_on: []
files_modified: ["searchboost_api/package.json", "searchboost_ui/package.json", "searchboost_warden/Cargo.toml", "searchboost_warden/src/relay.rs", "searchboost_service/searchboost_src/configurator.py"]
autonomous: true

must_haves:
  truths:
    - "Licenses match AGPL-3.0 strictly across all 3 backend segments"
    - "Code comments reflect actual Governor parameters"
    - "Local configurators appropriately match the exact docker-compose service names"
---

# Plan 6.1: Base Configurations & Licensing

<objective>
Patch the low-hanging fruit in basic structure logic without tearing down infrastructure: package licenses, documentation drift, and array variable typos.

Purpose: Brings the repository into proper legal standing and prevents local Docker-Compose host binding scripts from failing silently on SearXNG proxies.
</objective>

<tasks>

<task type="auto">
  <name>Align License Headers</name>
  <files>searchboost_api/package.json, searchboost_ui/package.json, searchboost_warden/Cargo.toml</files>
  <action>
    Change searchboost_api/package.json's "license" value from "ISC" to "AGPL-3.0".
    Add a "license": "AGPL-3.0" field to searchboost_ui/package.json.
    Add license = "AGPL-3.0" to searchboost_warden/Cargo.toml so all manifests match the project's AGPL-3.0 LICENSE.
  </action>
  <verify>grep "AGPL-3.0" searchboost_api/package.json searchboost_ui/package.json searchboost_warden/Cargo.toml</verify>
  <done>All three manifests contain AGPL-3.0 keys</done>
</task>

<task type="auto">
  <name>Correct Governor Limit Comments</name>
  <files>searchboost_warden/src/relay.rs</files>
  <action>
    Update the stale comment above the governor_conf configuration to reflect the actual rate-limit values being set.
    Replace "10 requests per second, with a burst fallback of 20" with "25 requests per second, with a burst fallback of 100".
  </action>
  <verify>grep "25 requests" searchboost_warden/src/relay.rs</verify>
  <done>Text aligns exactly with compiled runtime behavior</done>
</task>

<task type="auto">
  <name>Fix Typo in Local Configurator Script</name>
  <files>searchboost_service/searchboost_src/configurator.py</files>
  <action>
    Update the container_names array in the self._is_docker check block. Replace "sb_searxng" with "sb-searxng".
    AVOID deleting the whole block.
  </action>
  <verify>grep "sb-searxng" searchboost_service/searchboost_src/configurator.py</verify>
  <done>Local testing binds safely to the SearXNG web proxy port.</done>
</task>

</tasks>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
